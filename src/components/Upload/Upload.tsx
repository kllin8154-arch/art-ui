import React,{useState,useRef}from'react';import styles from'./Upload.module.css';export interface UploadFile{name:string;size:number;type:string;url?:string}interface UploadProps{accept?:string;multiple?:boolean;maxSize?:number;onChange?:(files:UploadFile[])=>void;className?:string}export function Upload({accept,multiple=false,maxSize,onChange,className=''}:UploadProps){const[files,setFiles]=useState<UploadFile[]>([]);const ref=useRef<HTMLInputElement>(null);const fmt=(b:number)=>b<1024?`${b}B`:b<1048576?`${(b/1024).toFixed(1)}KB`:`${(b/1048576).toFixed(1)}MB`;const h=(e:React.ChangeEvent<HTMLInputElement>)=>{const fs=Array.from(e.target.files||[]);if(maxSize){for(const f of fs){if(f.size>maxSize){alert(`${f.name} exceeds ${fmt(maxSize)}`);return}}}const nf:UploadFile[]=fs.map(f=>({name:f.name,size:f.size,type:f.type}));setFiles(p=>{const next=multiple?[...p,...nf]:nf;onChange?.(next);return next})};const rm=(i:number)=>{setFiles(p=>{const n=p.filter((_,j)=>j!==i);onChange?.(n);return n})};const isImg=(t:string)=>t.startsWith('image/');return <div className={`${styles.w} ${className}`}><input ref={ref} type="file" accept={accept} multiple={multiple} onChange={h} className={styles.inp}/><div className={styles.z} onClick={()=>ref.current?.click()} onDragOver={e=>{e.preventDefault();e.currentTarget.classList.add(styles.dr)}} onDragLeave={e=>e.currentTarget.classList.remove(styles.dr)} onDrop={e=>{e.preventDefault();e.currentTarget.classList.remove(styles.dr);if(e.dataTransfer.files.length){const dt=new DataTransfer();Array.from(e.dataTransfer.files).forEach(f=>dt.items.add(f));const inp=ref.current;if(inp){inp.files=dt.files;inp.dispatchEvent(new Event('change',{bubbles:true}))}}}><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" className={styles.ic}><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="17 8 12 3 7 8"/><path d="M12 3v12"/></svg><span className={styles.tx}>点击或拖拽上传</span></div>{files.length>0&&<div className={styles.lst}>{files.map((f,i)=><div key={i} className={styles.fi}>{isImg(f.type)&&f.url?<img src={f.url} className={styles.th}/>:<span className={styles.th} style={{background:'var(--art-color-bg-secondary)',display:'flex',alignItems:'center',justifyContent:'center',color:'var(--art-color-text-muted)'}}>FILE</span>}<div className={styles.m}><div className={styles.nm}>{f.name}</div><div className={styles.sz}>{fmt(f.size)}</div></div><button className={styles.rm} onClick={()=>rm(i)}><svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5"><path d="M18 6 6 18"/><path d="m6 6 12 12"/></svg></button></div>)}</div>}</div>}
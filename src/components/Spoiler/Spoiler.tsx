import React,{useState}from'react';import styles from'./Spoiler.module.css';interface SpoilerProps{title:React.ReactNode;children:React.ReactNode;defaultOpen?:boolean;className?:string}export function Spoiler({title,children,defaultOpen=false,className=''}:SpoilerProps){const[open,setOpen]=useState(defaultOpen);return <div className={`${styles.s} ${open?styles.open:''} ${className}`}><button className={styles.tr} onClick={()=>setOpen(!open)} type="button"><span className={styles.t}>{title}</span><svg className={`${styles.ch} ${open?styles.chOpen:''}`} width="14" height="14" viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round"><path d="M4 6l4 4 4-4"/></svg></button><div className={styles.p}><div className={styles.c}>{children}</div></div></div>}
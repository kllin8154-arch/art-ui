import React from'react';import styles from'./Text.module.css';type TV='body'|'small'|'muted'|'code'|'lead';interface TextProps{children:React.ReactNode;variant?:TV;as?:keyof JSX.IntrinsicElements;className?:string}export function Text({children,variant='body',as:Tag='p',className=''}:TextProps){return <Tag className={`${styles.t} ${styles[variant]} ${className}`}>{children}</Tag>}
import React from'react';import styles from'./List.module.css';interface ListProps{children:React.ReactNode;type?:'unordered'|'ordered';gap?:number|string;className?:string}export function List({children,type='unordered',gap,className=''}:ListProps){const Tag=type==='ordered'?'ol':'ul';const s:React.CSSProperties={};if(gap!==undefined)s.gap=typeof gap==='number'?`${gap}px`:gap;return <Tag className={`${styles.l} ${className}`} style={s}>{children}</Tag>}
import React from'react';import styles from'./ColorSwatch.module.css';interface ColorSwatchProps{color:string;size?:number;label?:string;className?:string}export function ColorSwatch({color,size=32,label,className=''}:ColorSwatchProps){return <span className={`${styles.w} ${className}`}><span className={styles.sw} style={{backgroundColor:color,width:size,height:size,borderRadius:size<=24?4:8}}/>{label&&<span className={styles.l}>{label}</span>}</span>}
import React,{useState,useEffect}from'react';interface CountdownProps{target:Date;className?:string}
export function Countdown({target,className=''}:CountdownProps){const[now,setNow]=useState(Date.now());useEffect(()=>{const t=setInterval(()=>setNow(Date.now()),1000);return()=>clearInterval(t)},[]);const diff=Math.max(0,Math.floor((target.getTime()-now)/1000));const d=Math.floor(diff/86400),h=Math.floor((diff%86400)/3600),m=Math.floor((diff%3600)/60),s=diff%60;return <div className={className} style={{display:'flex',gap:12,fontFamily:'var(--art-font-display)'}}><span style={{fontSize:'2rem',fontWeight:700}}>{String(d).padStart(2,'0')}</span><span style={{fontSize:'.75rem',color:'var(--art-color-text-muted)',alignSelf:'flex-end',marginBottom:4}}>天</span><span style={{fontSize:'2rem',fontWeight:700}}>{String(h).padStart(2,'0')}:{String(m).padStart(2,'0')}:{String(s).padStart(2,'0')}</span></div>;}
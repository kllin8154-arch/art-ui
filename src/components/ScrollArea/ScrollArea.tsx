import React from 'react';import styles from'./ScrollArea.module.css';export interface ScrollAreaProps{height?:number|string;maxHeight?:number|string;children:React.ReactNode;className?:string}export function ScrollArea({height,maxHeight,children,className=''}:ScrollAreaProps){const s:React.CSSProperties={};if(height!==undefined)s.height=typeof height==='number'?`${height}px`:height;if(maxHeight!==undefined)s.maxHeight=typeof maxHeight==='number'?`${maxHeight}px`:maxHeight;return <div className={`${styles.wrap} ${className}`} style={s}><div className={styles.inner}>{children}</div></div>}

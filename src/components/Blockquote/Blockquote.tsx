import React from'react';import styles from'./Blockquote.module.css';interface BlockquoteProps{children:React.ReactNode;cite?:string;className?:string}export function Blockquote({children,cite,className=''}:BlockquoteProps){return <blockquote className={`${styles.bq} ${className}`}><div className={styles.c}>{children}</div>{cite&&<footer className={styles.ct}>{cite}</footer>}</blockquote>}
import React,{useState,useRef,useEffect}from'react';import styles from'./Affix.module.css';interface AffixProps{children:React.ReactNode;top?:number;className?:string}export function Affix({children,top=0,className=''}:AffixProps){const[fixed,setFixed]=useState(false);const ref=useRef<HTMLDivElement>(null);const ph=useRef<HTMLDivElement>(null);useEffect(()=>{const el=ref.current,p=ph.current;if(!el||!p)return;const h=()=>{setFixed(p.getBoundingClientRect().top<=top)};window.addEventListener('scroll',h,{passive:true});window.addEventListener('resize',h);h();return()=>{window.removeEventListener('scroll',h);window.removeEventListener('resize',h)}},[top]);return <><div ref={ph}/><div ref={ref} className={`${fixed?styles.f:''} ${className}`} style={fixed?{top:`${top}px`}:undefined}>{children}</div></>}
import React,{useState}from'react';import styles from'./Image.module.css';interface ImageProps{src:string;alt:string;fallback?:string;width?:number|string;height?:number|string;fit?:'cover'|'contain'|'fill'|'none';radius?:number|string;className?:string}export function Image({src,alt,fallback,width,height,fit='cover',radius,className=''}:ImageProps){const[e,setE]=useState(false);const s:React.CSSProperties={};if(width)s.width=typeof width==='number'?`${width}px`:width;if(height)s.height=typeof height==='number'?`${height}px`:height;if(radius)s.borderRadius=typeof radius==='number'?`${radius}px`:radius;return <div className={`${styles.w} ${className}`} style={s}>{e&&fallback?<div className={styles.fb}>{fallback}</div>:<img src={src} alt={alt} className={styles.img} style={{objectFit:fit}} onError={()=>setE(true)} loading="lazy"/>}</div>}
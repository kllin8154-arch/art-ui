import React,{useEffect}from'react';import styles from'./Overlay.module.css';interface OverlayProps{visible:boolean;onClick?:()=>void;zIndex?:number;opacity?:number;className?:string}export function Overlay({visible,onClick,zIndex,opacity,className=''}:OverlayProps){useEffect(()=>{document.body.style.overflow=visible?'hidden':'';return()=>{document.body.style.overflow=''}},[visible]);const s:React.CSSProperties={};if(zIndex)s.zIndex=zIndex;if(opacity)s.background=`rgba(0,0,0,${opacity})`;return <div className={`${styles.o} ${visible?styles.show:''} ${className}`} style={s} onClick={onClick}/>}
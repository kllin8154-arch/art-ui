import React from'react';import styles from'./Indicator.module.css';type IndicatorColor='accent'|'green'|'yellow'|'red'|'gray';interface IndicatorProps{color?:IndicatorColor;size?:number;pulse?:boolean;label?:string;className?:string}export function Indicator({color='accent',size=10,pulse=false,label,className=''}:IndicatorProps){return <span className={`${styles.w} ${className}`}><span className={`${styles.d} ${styles[color]} ${pulse?styles.pulse:''}`} style={{width:size,height:size}}/>{label&&<span className={styles.l}>{label}</span>}</span>}
import React,{useRef,useState}from'react';import styles from'./FileInput.module.css';interface FileInputProps{accept?:string;multiple?:boolean;onChange?:(files:File[])=>void;placeholder?:string;disabled?:boolean;className?:string}export function FileInput({accept,multiple=false,onChange,placeholder='选择文件...',disabled=false,className=''}:FileInputProps){const[files,setFiles]=useState<File[]>([]);const ref=useRef<HTMLInputElement>(null);const h=(e:React.ChangeEvent<HTMLInputElement>)=>{const fs=Array.from(e.target.files||[]);setFiles(fs);onChange?.(fs)};const fmt=(b:number)=>b<1024?`${b}B`:b<1048576?`${(b/1024).toFixed(1)}KB`:`${(b/1048576).toFixed(1)}MB`;return <div className={`${styles.w} ${disabled?styles.dis:''} ${className}`}><input ref={ref} type="file" accept={accept} multiple={multiple} onChange={h} disabled={disabled} className={styles.inp}/><div className={styles.b} onClick={()=>ref.current?.click()}><svg className={styles.ic} width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="17 8 12 3 7 8"/><path d="M12 3v12"/></svg>{files.length===0?<span className={styles.ph}>{placeholder}</span>:files.map((f,i)=><span key={i} className={styles.fn}>{f.name}<span className={styles.fs}>{fmt(f.size)}</span></span>)}</div></div>}